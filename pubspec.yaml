
name: census2026
description: تطبيق إحصاء السكن الريفي 2026
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # تم تفعيل المكتبات (إزالة #) لكي لا ينهار التطبيق
  sqflite: ^2.3.0
  path: ^1.9.0
  file_picker: ^8.0.0
  excel: ^4.0.3
  permission_handler: ^11.0.0
  flutter_inappwebview: ^6.0.0
  share_plus: ^10.0.0
  archive: ^3.6.1
  path_provider: ^2.1.0
  image: ^4.2.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/
    - assets/app.html
