name: Build Release APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest

    steps:
      - name: 📥 استنساخ المشروع
        uses: actions/checkout@v4

      - name: ☕ إعداد Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🐦 إعداد Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: 📦 تثبيت الحزم
        run: flutter pub get

      - name: 🔧 توليد منصة Android
        run: flutter create --platforms=android .

      - name: 🗑️ حذف ملفات الكاش القديمة لتجنب التعارضات
        run: |
          rm -rf ~/.gradle/caches/
          rm -rf android/.gradle
          rm -rf android/app/build

      - name: 📥 تثبيت Android SDK Platform 35 و Build-Tools 35.0.0
        run: |
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-35" "build-tools;35.0.0"
          flutter config --android-sdk $ANDROID_HOME

      - name: ⚙️ ضبط compileSdk 35 و Kotlin 1.9.24 في جميع الملفات
        run: |
          # 1. app/build.gradle
          sed -i 's/compileSdk = .*/compileSdk = 35/' android/app/build.gradle
          sed -i 's/targetSdk = .*/targetSdk = 35/' android/app/build.gradle
          
          # 2. build.gradle (جذر المشروع)
          sed -i "s/ext.kotlin_version = .*/ext.kotlin_version = '1.9.24'/" android/build.gradle
          sed -i 's/classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:.*"/classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.24"/' android/build.gradle
          
          # 3. settings.gradle (للمشاريع الحديثة)
          if [ -f "android/settings.gradle" ]; then
            sed -i 's/kotlin("android") version ".*"/kotlin("android") version "1.9.24"/' android/settings.gradle
          fi
          
          # 4. gradle-wrapper.properties (ترقية Gradle إلى 8.3 لضمان التوافق)
          sed -i 's/distributionUrl=.*/distributionUrl=https\\:\/\/services.gradle.org\/distributions\/gradle-8.3-all.zip/' android/gradle/wrapper/gradle-wrapper.properties

      - name: ✏️ إضافة أذونات الكاميرا والتخزين
        run: |
          MANIFEST_PATH="android/app/src/main/AndroidManifest.xml"
          sed -i '/<application/i \
              <uses-permission android:name="android.permission.CAMERA" /> \
              <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> \
              <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />' $MANIFEST_PATH

      - name: 🎨 نسخ أيقونة التطبيق (إن وجدت)
        run: |
          if [ -f "assets/icon.png" ]; then
            mkdir -p android/app/src/main/res/mipmap-hdpi
            mkdir -p android/app/src/main/res/mipmap-mdpi
            mkdir -p android/app/src/main/res/mipmap-xhdpi
            mkdir -p android/app/src/main/res/mipmap-xxhdpi
            mkdir -p android/app/src/main/res/mipmap-xxxhdpi
            cp assets/icon.png android/app/src/main/res/mipmap-hdpi/ic_launcher.png
            cp assets/icon.png android/app/src/main/res/mipmap-mdpi/ic_launcher.png
            cp assets/icon.png android/app/src/main/res/mipmap-xhdpi/ic_launcher.png
            cp assets/icon.png android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png
            cp assets/icon.png android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png
          fi

      - name: 🔐 صلاحيات تنفيذ gradlew
        run: chmod +x android/gradlew

      - name: 🧹 تنظيف Flutter
        run: flutter clean

      - name: 🔨 بناء APK Release
        run: flutter build apk --release --no-tree-shake-icons

      - name: 📤 رفع APK
        uses: actions/upload-artifact@v4
        with:
          name: census2026-release
          path: build/app/outputs/flutter-apk/app-release.apk
